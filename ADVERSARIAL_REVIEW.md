# 对抗式审查记录

审查日期：2026-09-15（Asia/Shanghai）。范围为 `UART-elec` 联控与 `UART` 分机的本次重构，按实际生产调用链、故障注入和寄存器时序寻找反例；已有测试通过不作为否定缺陷的依据。

## 已证实并修复的反例

| 级别 | 触发条件与原后果 | 修复及回归依据 |
| --- | --- | --- |
| P1 | 远控期间 KP35 输入停热，本地压力调节分支被跳过，三组仍开 | 远控功率与本地停热许可分开，真实解析器、控制及 GPIO 组合回归 |
| P1 | 远控下超过本地自动停止压力但未达硬超压值，继续加热 | 保留本地启停迟滞与远控需求，压力下降后可正常恢复；重复功率包不能绕过停热 |
| P1 | LCD STOP 后，下一条普通 START=1 功率包重新开热 | 本地 STOP 撤销远控并锁存，须有效停止再启动或明确本地启动 |
| P1 | 远控会话内物理 START 从高变低，旧代码直接返回忽略 | 记录会话中观察到的高输入，下降边沿立即停止并锁存；初始低电平仍可远控 |
| P1 | 只有节点 1 持续改功率，80 次事务读写全部落在节点 1，节点 2–4 饥饿 | 独立在线读游标与有限插队额度；另测十节点、持续 STOP 变化下的有界轮询 |
| P1 | 连续合法异常应答仍清失败计数、保留旧运行健康状态 | 异常立即撤销控制资格，保留真实故障和未确认命令；有效 03 状态读取后恢复 |
| P1 | 配置地址移除再加入沿用旧 ACK、状态或活动事务 | 成员边沿清理旧会话并终止被移除节点事务，重新加入先读后写 |
| P2 | 分机刚接收 START 即报告运行，尚在补水却被计作可供热容量 | 首次中水位确认后才计入；正常补水低水仍在可保持，低水丢失或非法水位清除准备状态 |
| P2 | TX 中止清 TE，实际冻结发送而不是允许剩余字节排空 | 仅停 DMA/DMAT/TCIE，保持 TE 排空；真实适配器函数的中止、watchdog、DMA/TC 与错误回归 |
| P2 | 合法 t3.5 连续两帧在 100 微秒采样下被全部丢弃 | 按 200 微秒不确定区间判断，100 种采样相位验证合法 t1.5/t3.5，保留确定非法间隔和漏采样丢弃 |
| P2 | 旧 LCD 报文逐字节入队，两项队列满后截断报文；CPU ID 启动日志同类问题 | 六个 LCD builder 改整帧复制，逐字节比对原报文和长度；启动日志改整串入队 |

保护组合测试由原 5 项通过、4 个新反例失败转为 9 项全部通过。UART4 测试由原 16 项通过、3 个新反例失败转为 20 项通过，包含新增十节点压力场景。其他新增测试同样先验证反例失败，再验证修复。

TE 的判断依据为 ST [RM0008 第 27.3.2 节，791–793 页](https://www.st.com/resource/en/reference_manual/rm0008-stm32f103xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf)：发送中清 TE 会冻结波特率计数并损坏当前发送，因此不能据此声称数据寄存器与移位寄存器已排空。

## 复现与验证入口

两仓共同运行：

```text
python scripts/build_firmware.py
python scripts/run_host_tests.py
python tests/test_serial_dma_adapter.py
python tests/test_legacy_tx.py
```

联控追加：

```text
python tests/test_control_regressions.py
python tests/test_control_regressions.py test_control_stages.c
python tests/test_modbus_scheduler.py
```

分机追加：

```text
python tests/test_control_slave.py
python tests/test_control_parser.py
```

## 审查与软件验证不能证明的事项

- ARM GCC 检查只编译原 Keil 工程列出的 C 文件，未完成启动汇编、完整链接或烧录。需核对 F103VE/C8 的 Flash、RAM、栈及中断向量。
- 主机寄存器模型不能证明实际 DE 最后停止位、DMA 仲裁、Flash 操作期间中断延迟或探头/接触器响应；须上板验证。
- 100 微秒采样采用 200 微秒时间容差，无法同时精确区分误差窗内所有合法与非法边沿。
- 现有运行/水位寄存器仅能推断准备度，不报告实际接触器、电流或分机内部全部等待状态。
- RTU 没有事务编号，隔离期后与新请求完全同构的旧应答仍无法仅靠线上内容区分。
- 原联控 LCD UART2 的地址与部分字段宽容规则保留。调试 `fputc` 为尽力发送，队满返回 `EOF`，不能用于构造协议报文。

本轮已证实的上述阻塞缺陷均有修复及软件回归；硬件验收仍待执行。
