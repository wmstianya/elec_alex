# 联控与分机配套重构记录

本目录是版本配套仓库，`UART-elec` 和 `UART` 保持 Git 子模块结构（索引模式 `160000`）。联控源码、测试、工程配置在 `UART-elec` 独立提交；分机对应内容在 `UART` 独立提交。父仓库记录配套版本与此验证说明。

## 设备与本次边界

- 每台容积约 30 L，三组独立加热，每组 24 kW，单台合计 72 kW。
- 保留现有联控架构、9600 baud / 8N1、站号和 Modbus 寄存器含义。
- 改善确定性 PI 计算、三组离散功率分配、分机保护优先级以及串口收发/事务管理。压力参数和接触器长期投切周期仍需要设备数据，不在本次代码中推定。

## 提交分组

1. 两个子仓库分别增加构建/测试入口与变更说明。
2. 联控修复 PI 周期重复执行、容量整数溢出、积分启停/饱和及三台功率守恒。
3. 联控增加 24 kW 整数组分配；分机单独提交三组译码及保护逻辑。
4. 两个子仓库分别提交 DMA 串口传输与原协议接入。
5. 联控单独提交 Modbus 主站事务调度。
6. 父仓库更新经过验证的配套子模块版本。

## 本次配套版本

分支均为 `codex/refactor-discrete-heat-dma`，提交身份为 `alex <wmstianya@gmail.com>`。

| 子仓库 | 锁定提交 | 大改提交 |
| --- | --- | --- |
| 联控 `UART-elec` | `a0d9cd9eee958467880fa698571212fe349f7768` | `41822ab` 构建、`febbe3b` PI、`f48c77a` 离散分配、`01a6ad3` DMA、`d499e2c` 主站事务 |
| 分机 `UART` | `4adbcd8b8f7a3b9f559ed64983edd4de5efed18c` | `299ad6b` 构建、`2db468f` 加热保护、`bc3391f` DMA与解析器 |

两个锁定提交另包含验证与提交索引文档。父仓库保留两个 `160000` gitlink，不把独立固件源码合并进父仓库。此次仅创建本地提交，未推送或烧录。

## 软件验证入口

在各子仓库目录使用 Python 运行：

```text
python scripts/build_firmware.py
python scripts/run_host_tests.py
```

控制与调度的集成回归直接提取当前生产 C 函数编译运行，入口列在对应子仓库的变更验证说明中。主机测试使用本机 GCC；ARM 编译根据原 `USER/UART.uvprojx` 的源文件、包含目录和宏执行。

原始基线为联控 52/52、分机 52/52 个 C 翻译单元编译通过。此检查不编译 Keil 启动汇编，不链接完整固件，因此不证明最终 Flash/RAM 占用、向量链接或可烧录镜像。日志及对象文件位于各子仓库被忽略的 `build/`。

2026-09-15（Asia/Shanghai）最终软件验证结果：

| 检查 | 联控 `UART-elec` | 分机 `UART` |
| --- | --- | --- |
| ARM C 翻译单元编译 | 57/57 通过 | 56/56 通过 |
| 通用主机测试入口 | 3/3 套件通过 | 3/3 套件通过 |
| 生产控制函数回归 | PI/整数运算、离散分配回归通过 | 三组译码、保护及继电器回归通过 |
| 生产通信与控制组合回归 | UART4 调度 20/20 场景通过 | 解析器/控制/继电器组合 9/9 场景通过 |
| DMA 硬件适配器主机回归 | 中止、超时、DMA/TC、DMA 错误通过 | 同左 |
| 旧 LCD 报文入队 | 六种报文及双报文队列通过 | 同左 |
| 对象文件强符号检查 | 无重复强符号 | 无重复强符号 |

两份串口公共模块的六个源/头文件内容一致，原 Keil 工程中的源文件路径均存在。对象符号检查不能代替启动向量与完整固件链接验证。控制、串口及调度的独立审查问题已修正并经过回归；以下上板项目仍待执行。

本次追加了对抗式审查，具体反例、修复与残余边界见 [ADVERSARIAL_REVIEW.md](ADVERSARIAL_REVIEW.md)。每个大改的提交内容均单独导出进行编译和对应回归，保证分层历史包含当时所需的源码、头文件及工程配置。

## 上板验证

- 使用原 Keil 工程完整编译、链接，核对 RAM/Flash、栈空间和 DMA/USART/TIM1 中断向量；两套 MCU 型号分别核对。
- 在原波特率下连续收发，覆盖最大合法帧、环回绕、短帧/错 CRC、队列满、串口错误、离线恢复及多路 ADC/串口并发。
- 用逻辑分析仪检查字节间隔、RTU 帧间隔边界和分机 RS485 DE。最后停止位结束后才能释放 DE；DMA 完成不代表发送完成。
- 检查 Flash 写入或长时间中断屏蔽后的丢帧计数、恢复与看门狗；定时采样按 200 微秒不确定区间保留合法边界，不能用主机测试证明精确线速时序。
- 联控运行、启动输入保持高时拔线，确认分机停止加热；普通 `StartFlag=1` 功率更新也不得解锁。远端需有效停止后再启动，或明确本地启动，才可解除锁存。
- 低水位、压力保护、故障、停机和手动模式分别检查三组实际 GPIO/接触器。命令百分比与实际加热/电流反馈分开核验。
- 在共同蒸汽负载及实际补水条件下记录压力曲线、三组投切次数和累计工作时间，再决定 PI 参数与最短投切周期。

Modbus RTU 没有事务编号。地址/功能/长度/CRC 校验和超时隔离可以排除不匹配、队列中已过期及隔离期内的应答；不能区分隔离期结束后出现的、与新请求完全同构的旧应答。写应答表示协议接收成功，设备状态以随后的读取及实际硬件测量核验。
