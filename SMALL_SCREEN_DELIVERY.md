# 分机小屏接口交付

本次只修改 `UART` 分机及总仓库配套指针，联控固件保持原版本。分支 `codex/slave-small-screen` 基于既有 `codex/refactor-discrete-heat-dma`，PR 也以该重构分支为目标，以便单独审核小屏修改。

分机实现已提交为 `61d1d94a4195d2fe38e7273d750233442c67424e`，对应[分机 PR #1](https://github.com/wmstianya/elec_steam_medium/pull/1)。总仓库的 `UART` 子模块指向该提交。

新增只读 Modbus RTU 实时接口，保持用户 CSV 的50个寄存器位置，补充泵/排污输出位图、三组加热输出和投入组数、排污阶段、真实阀位反馈的 UNKNOWN 状态及最终加热许可。采用显式高字节/高字在前的序列化，支持连续任意区间读取；故障码、状态、压力和配置值同一快照返回。

详见子仓库[接线与寄存器说明](UART/docs/slave-small-screen.md)。用户原始 CSV 及补充空白变量名的显示 CSV 在 `UART/docs/protocol/`。CSV 不含屏的页面、串口设备参数或字节交换选项，因此没有声称已完成 MCGS 屏工程配置。

## 已确认的端口

现场已确认只有接触器，没有可控硅调功器。当前默认 `SLAVE_SMALL_SCREEN_PORT=1`，普通构建即可启用小屏接口：

- 当前配置1：小屏使用 PA9/TX、PA10/RX、PA8/方向；停止同一路调功器轮询和调试输出，PA2/PA3/PA4 继续联控。
- 可选配置0恢复旧通信用途；配置2将小屏分配到 PA2/PA3/PA4 并停止该路联控，不用于本次接线。

端口用途已明确；9600/8N1、站号254及ABCD浮点顺序需要在屏上配套设置，CSV 不包含这些设备参数。建议每250～500ms读取一次。读小屏不会抢占联控、更新联控心跳或写入启停/参数。屏显示的继电器输出不能替代接触器电流/阀位实测反馈。软件默认启用不等于已完成实屏验收。

## 已执行的软件验证

- 分机 host suites：8项通过；小屏协议10组、全部1275种有效连续读取范围；适配7组，涵盖状态变更、写入拒绝、端口隔离、拥塞、过期及计时回绕。
- 生产 DMA 接收路由和主循环/调试打印：默认构建及显式0/1/2配置均验证；默认构建应停止调功器轮询、保留联控，在输出处理后响应小屏。旧代码在配置1会丢弃读屏请求的反例已复现。
- 生产控制/解析集成：19场景通过，包含真实通信停机后“小屏加热许可=0、加热输出=0、读屏不能重新允许旧START”。
- 既有控制、DMA发送适配、旧LCD发送、C8布局测试及总仓库主从排污协议往返回归通过。
- 三种端口配置分别完成61个ARM C对象编译，均0错误、0警告。未完成Keil完整链接、MCGS导入或实屏/实板验收。
- 独立规格复核及对抗式代码审核通过；审核范围为软件，未证明上板实时性或完整栈余量。
- 来源CSV的SHA256和所有42个通道地址/类型/驱动字段已核验；只补充9个空白名称。

本次不改变 Issue #1 的排污准入条件。仍需完整链接并检查60 KiB程序区、20 KiB RAM、栈余量和实际RS485时序，不能把软件测试作为可直接现场烧录的证明。
