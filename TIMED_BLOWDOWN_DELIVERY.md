# 自动排污定时开阀交付

本次以 `codex/slave-small-screen` 为基础，在 `codex/timed-auto-blowdown` 提交独立 PR；保留小屏站号2和两路独立串口。分机代码变更，联控子模块保持原提交。

分机提交：`ae7b8090cc8d2dbd36d4e4b8be0eb62ff8f87c70`；[分机 PR #2](https://github.com/wmstianya/elec_steam_medium/pull/2)。作者/提交者：`alex <wmstianya@gmail.com>`。此增量依赖既有[分机小屏 PR #1](https://github.com/wmstianya/elec_steam_medium/pull/1)及[总仓库小屏 PR #2](https://github.com/wmstianya/elec_alex/pull/2)，不将其混入本次新增 PR 范围。

## 已确认行为

自动排污请求取得共管许可后：停止加热 → 原有5秒停热检查 → 按固件参数开阀（默认10秒）→ 关阀及确认 → 补水、重新确认水位并稳定 → 原控制逻辑分级加热。

用户已确认“不检测液位”限定为：开阀期间暂停E3确认，保留原始采样/记录和停热保护。自动OPEN中水位低/组合波动不提前中止定时开阀；采样失效、失联/许可撤销、压力、停机、阀反馈丢失及加热输出重新出现仍会关阀。开阀结束立即恢复水位判断，既有E3不被清除。恢复加热不能使用排污前的水位资格。

时长先作为固件参数，不新增屏设置和断电保存。编辑 `UART/SYSTEM/system_control/slave_water_control.h` 的 `WATER_BLOWDOWN_OPEN_SECONDS`，默认10，编译校验范围1～60秒；也可通过同名编译宏覆盖。该范围不代表已验证的现场运行范围。手动继电器排污保持默认1.5秒与原水位中止逻辑。

退出OPEN后重新确认15秒稳定水位，可与实际闭阀等待重叠；确认闭阀且水位合格后，再经过原15秒排污稳定阶段。补水总上限240秒，不因抖动无限延长。当前时长是开阀命令阶段的时间，不是阀门实际全开时间。

## 校验与限制

已运行：分机8项host suites，真实控制解析19场景，继电器/超时控制回归，主从10秒排污、许可续期、热容量隔离与恢复回归；参数默认10秒、1/60秒覆盖及0/61/-1拒绝检查；默认61个ARM C对象编译0错误0警告。水位暂停独立测试覆盖954条断言；事件记录增加暂停标志后仍为20字节/条、640字节/环。

独立规格审核、主从协议核对及后续对抗式代码审核均通过，无本次增量的剩余阻断项；审核结论不包含硬件验收。

完整Keil链接、最终Flash/RAM与栈余量、实际接触器/阀门时序及工艺仍未上板验收。**仍保留原有排污准入：生产板级阀反馈UNKNOWN、会话0，不能因此直接开阀。** 本PR不伪造闭阀反馈，也不以许可到期推断真实关阀；这部分属于原Issue #1的剩余板级工作。

详细接口、手动/自动来源及测试命令见[分机说明](UART/docs/water-blowdown-stability.md)。实际排放量与压力、管路等有关，共管仍需逐台互锁，10秒应结合实测核定。[参考：排污与共管协调](https://www.spiraxsarco.com/learn-about-steam/the-boiler-house/bottom-blowdown)
