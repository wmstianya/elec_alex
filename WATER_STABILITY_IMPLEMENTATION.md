# Issue #1：水位保护与共管排污的软件实现

关联：https://github.com/wmstianya/elec_alex/issues/1

配套分支：`codex/refactor-discrete-heat-dma`；作者/提交者：`alex <wmstianya@gmail.com>`。

| 固件 | 本轮提交 | 修改前配套提交 |
| --- | --- | --- |
| 分机 `UART` | `923d837e804780d2974d9ad812a2fcb7c17e5683` | `4adbcd8b8f7a3b9f559ed64983edd4de5efed18c` |
| 联控 `UART-elec` | `c031042c5b5e27e921ae1833e33f99d23c22d111` | `a0d9cd9eee958467880fa698571212fe349f7768` |

## 当前交付边界

本轮实现水位判定、分机排污状态机、主控唯一许可、协议及供热容量协调，并加入反例和真实控制入口回归。尚未上板、烧录或完成现场验收，不能把本次软件修改等同于现场 E3 根因已排除。

**当前生产接线适配仍报告阀位 UNKNOWN，启动会话为 0，不发放排污许可。所有自动、远程及本地手动开阀均被阻止；正常加热与补水仍按水位及既有保护运行。这个开发版本不适合直接替换现场固件。** 排污启用前必须补齐实际闭阀证据、持久化会话与整机验收，不能把 `Switch_Inf.pai_wu_flag` 当成到位反馈，也不能仅调用会话绑定函数跳过调试准入。

TIM3 保留原配置。已确认源码中 PA6、`TIM_OC4Init/TIM_OC4PreloadConfig` 与运行时 `TIM3->CCR1` 不一致；实际引脚、原理图及激励波形未核实，未据此改通道或认定现场原因。

## STM32F103C8T6 补充核查

用户补充芯片为 STM32F103C8T6；据此对已有 C8 目标的分机仓库按额定 64 KiB Flash / 20 KiB SRAM 核查。联控仓库当前配置 F103VE，不能仅凭分机配置将其改成 C8；两板型号的适用范围仍待分别确认。[ST 数据手册](https://www.st.com/resource/en/datasheet/stm32f103c8.pdf)

发现并修正分机程序区与参数区可能重叠的问题：`Write_Internal_Flash()` 擦除起点为 `0x0800F000`，原 Keil 程序区却覆盖完整 64 KiB。现在 IROM / OCR_RVCT4 都限制为前 60 KiB，最后 4 KiB 留在程序区之外，原参数地址保持不变。构建检查验证内存区域与自动链接配置，反例覆盖 Flash/RAM 越界、区域不一致、额外区域及自定义链接选项。

补充修复：分机提交 `923d837e804780d2974d9ad812a2fcb7c17e5683`，在原水位/排污提交 `8287eb3` 上继续。3 项布局测试（含 12 个非法配置反例）及 59 个 ARM C 对象编译通过，0 错误、0 警告；独立对抗式复核未发现本次配置修改的代码阻断项，并修正了下载器范围的文档表述。

这个修改是链接布局保护，尚不证明最终固件能装入 60 KiB，也不保证下载器全片擦除保留参数；必须继续完成目标链接和烧录流程核验。独立复核还确认旧下载选项使用 `STM32F10x_128` / `-FL020000`（128 KiB），尚不能视为 C8 的实际下载/擦除范围已经核验。保留区尚未分配会话持久化，原有超出 64 KiB 的历史地址不能用于新增存储。PA6 的默认复用 TIM3_CH1 已由手册核对，PCB 接线和实际波形仍待验证，TIM3 尚未改动。

## 分机实现

- `water_level_core.c/.h`：独立维护原始组合、确认状态、异常计时、数据新鲜度、热许可。有效几何组合为 0、1、3、7；0 仍禁止加热。
- `bsp_parallel_serial.c`：低、中、高电极从同一次 GPIOA 快照取值。普通输入按最小采样间隔送入模块，影响停热、报警连续性和恢复连续性的边沿立即送入，不借用通用引脚扫描的延时。
- `Slave_Control_HeatAllowed()` 与最终加热继电器入口：低水位丢失、非法组合、采样停滞立即撤销热许可；稳定恢复后按既有逐级约束投入。停止、压力及通信保护仍优先。
- `blowdown_core.c/.h`：等待许可 → 检查并停热 → 开阀 → 关阀确认 → 补水 → 连续稳定 → 完成。预备、开阀、闭阀、补水和恢复计时分别维护，阀命令关闭不代表物理闭阀。补水超时不是成功。
- `slave_water_control.c/.h`：统一自动、远程、LCD 与手动请求、按来源上升沿去重，维护许可会话/请求号/序号、故障及有界 RAM 事件记录。普通功率刷新和一直为 1 的排污标志不会重新触发已处理请求。
- 手动泵操作受排污泵策略限制；开阀的最终继电器函数再次检查许可。现有手动继电器排污位是掩码 `0x0002`。

短暂非法组合先停热；E3 只由持续非法组合确认。排污中出现非法组合还会立即中止排污，保留中止原因。已经开始的中止请求需确认闭阀；没有确认 E3 的中止也可能停留在排污故障阶段，允许显式复位，但复位后仍须重新确认水位。需要在现场 HMI 展示新增阶段/中止原因，不能仅依赖旧的单一故障码。采样停滞单独记录并停热，不伪装成电极组合 E3。

## 主控实现

`union_blowdown_core.c/.h` 独立维护最多 10 个节点的请求、会话、命令序号、读回时间、完成证据和唯一所有者。轮询公平选择请求；必须所有配置在同一总管上的节点都提供新鲜、已绑定且安全的状态，才可发放许可。任何关联设备的开阀/未知状态都不能通过移出配置消除。

`usart4.c` 从通过 CRC、长度、地址和当前事务验证的读回应答中更新状态。写入 ACK 不刷新阀位/水位，也不证明排污完成。序列化与重传保持相同许可内容；新的续期需要上一序号已读回。许可撤销后取消尚未发送完成的旧许可报文。已经退出联控配置的排污所有者保留限频的 STOP/CANCEL 和状态读取通路，直到获得闭阀证据；它不会因此重新获得供热资格。

`system_control.c` 将排污、恢复、故障和已识别新版节点的过期/未准备状态从供热分配中扣除。PI 的上限随可用容量变化，容量为零时清除积分。仅在当前容量分配快照证明剩余加热级数足够时选择新排污所有者；后台通信只续期或撤销已有所有者，不使用旧的容量判断新增许可。等待许可本身不禁止正常供热。

## B1 协议映射

保留 Modbus RTU 读 11 个寄存器、写 10 个寄存器以及原地址、CRC 和帧长。使用已有保留字，不修改 LCD 联合体结构。字内为 Modbus 大端。

| 方向 | 寄存器 | 含义 |
| --- | --- | --- |
| 写 | 106 | 高字节 B1；低字节命令：0 中性/撤权、1 SYNC、2 GRANT、4 CANCEL |
| 写 | 107 | 非零、已持久化且不重复使用的会话编号 |
| 写 | 108 | 本次分机请求编号；GRANT 必须精确匹配 |
| 写 | 109 | 命令序号；重复报文不续期，计数耗尽不回绕复用 |
| 读 | 103 | 高字节 B1；低四位阶段；bit4..5 阀位；bit6 热许可；bit7 已绑定会话 |
| 读 | 106 | 分机会话 |
| 读 | 108 | 分机当前/最近请求编号 |
| 读 | 109 | 分机已接受的命令序号 |

压力仍在读寄存器 101，设备类型仍在 110，不占用它们。阶段依次为 IDLE=0、WAIT_GRANT=1、STOP_HEAT=2、OPEN=3、CLOSE=4、REFILL=5、RECOVER=6、COMPLETE=7、FAULT=8。阀位 UNKNOWN=0、CLOSED=1、OPEN=2。

中性报文不取消一个尚未获授权的新排队请求，避免上一请求完成回执与下一请求交错。CANCEL 一律撤销开阀许可。未开始请求可以安全退回 IDLE，但 IDLE 不作为物理闭阀或排污成功证明。已选中所有者却退回 IDLE 时，主控保持互锁，需重新核验。

| 混用场景 | 行为与限制 |
| --- | --- |
| 新主控、新分机，未调试准入 | 旧加热寄存器可用；排污关闭；新版热许可和阶段参加容量判断 |
| 新主控、旧分机 | 保留既有加热通讯；屏蔽主控发出的排污请求及手动开阀位；不能阻止旧分机本地旧程序自行排污，因此不允许宣称共管已互锁 |
| 旧主控、新分机 | 保留加热通讯；旧排污命令只能成为未授权请求，不能开阀 |
| 全部新固件、已调试准入 | 只有匹配会话/请求/序号且仍有效的许可可开阀；移交依赖匹配的闭阀终态及恢复证据 |
| 未知或非法非零扩展 | 拒绝不安全副作用、撤销排污许可；STOP 仍执行 |

绑定接口仅供经验证的板级调试准入层调用，不能通过接收一个 SYNC 报文生成或持久化会话。任一参与者重启需要持久化的新会话及完整状态核验。协调器不能凭许可过期推断阀已经关闭。

## 参数与记录

| 参数 | 当前开发值 | 限制 |
| --- | --- | --- |
| 普通采样最小间隔 | 10 ms | 前台读取可能有抖动，重要边沿立即处理；需台架测量实际周期 |
| 采样失效 | 100 ms | 超时撤销热许可 |
| E3 连续确认/水位稳定恢复 | 15 s / 15 s | 沿用现有运行等待量级，未按现场波形调参；即时停热不等待此时限 |
| 运行缺水报警/启动补水上限 | 60 s / 240 s | 原缺水及补水量级；立即停热独立 |
| 停热稳定/预备阶段上限 | 5 s / 15 s | 开发默认，需确认接触器及反馈响应 |
| 开阀上限/闭阀确认上限 | 1.5 s / 15 s | 开发默认，排污仍禁用；需工艺和阀行程验证 |
| 排污后补水总上限/稳定时间 | 240 s / 15 s | 稳定被打断不延长总上限 |
| 许可有效期/主控续期周期 | 20 s / 5 s | 要验证十台轮询、重试及总线最差延迟 |

事件记录固定 32 条（640 字节），保留原始/确认水位、压力、请求号、阶段、排污泵阀策略命令、阀反馈、加热命令级数、首个报警及中止原因；确认故障时冻结。缓冲区满后覆盖最旧事件并累计覆盖数量，计数饱和而不回绕。记录在 RAM 中，不高频擦写 Flash，也不会在普通复位时清掉证据。正常补水泵的完整动作记录与导出界面仍需接入。采样与过程波形仍需外部采集，有限环形记录不能替代示波器或现场数据记录器。

## 校验入口

在两个子仓库分别运行：

```text
python scripts/run_host_tests.py
python scripts/build_firmware.py
python tests/test_serial_dma_adapter.py
python tests/test_legacy_tx.py
```

主控追加：

```text
python tests/test_control_regressions.py
python tests/test_control_regressions.py test_control_stages.c
python tests/test_modbus_scheduler.py
```

分机追加：

```text
python tests/test_flash_layout.py
python tests/test_control_slave.py
python tests/test_control_parser.py
```

总仓库追加配套协议回归：

```text
python tests/test_blowdown_protocol.py
```

测试直接编译纯控制模块及当前生产控制/解析入口；保留通信超时、STOP、压力优先、功率分级和 DMA 回归。对抗式审核的回归包括：许可跨请求继承、到期前处理顺序、未知阀位、未开始请求取消、异常/正常快速交替、闭阀续期、配置移除、撤权后的待发帧及旧状态/ACK。

本轮检查结果：两仓清单中的主机测试分别为分机6项、主控4项；分机真实解析/控制集成18场景、主控UART4调度25场景及配套协议往返回归通过。既有控制、分级、DMA适配及LCD发送回归通过。ARM C对象检查为分机59个、主控58个，均无编译错误和警告；没有执行完整固件链接或烧录。

独立对抗式审核发现的反例均加入回归并修复；最后一次复核未发现剩余软件阻断项。配套回归还验证了完成后绑定新会话时输出一致的 IDLE/request=0 状态，避免主控将其长期判为不可用。审核通过范围仅为本次软件与测试，不包括下述硬件、存储布局和现场验收。

## 仍需完成的调试与验收

1. 核实板卡及阀门反馈；无实际反馈时设计并验证独立互锁/闭阀确认方案，不用延时猜测闭阀。
2. 确定安全的会话持久化布局和掉电原子性，补齐板级启动/恢复绑定。C8 程序区已限制为 60 KiB，最后 4 KiB 排除出程序区；既有历史管理员/激活地址仍有超出物理 64 KiB 的定义，不能盲目迁移或新增 Flash 地址。联控存储布局需单独核对。
3. 使用目标工程完整链接，检查代码、数据、栈、中断向量以及配置区边界。当前 ARM GCC 检查只编译 C 对象，不处理原 ARMASM 启动文件或完成 Keil 链接。
4. HMI 显示排队、容量不足、排污中止及互锁原因；导出事件记录。旧界面不能完整表达 B1 新增状态。
5. 取得 TIM3 激励及三路检测、压力、阀泵动作的同步波形，确定生产时限与压力范围。
6. 台架故障注入及单机 → 少量共管 → 全机验证；核对总管能力、防倒流、真实缺水保护和压力恢复，保留回退版本。

这些项目未完成前，Issue 保持开放，不发布为已经完成物理互锁或现场 E3 消除的固件。
